ext {
    WEB_COMMON_MODULE = ":web-common"
}

dependencies {
    implementation project(WEB_COMMON_MODULE)
    
    testImplementation project(WEB_COMMON_MODULE).sourceSets.test.output
    testRuntimeOnly("com.h2database:h2")
}
